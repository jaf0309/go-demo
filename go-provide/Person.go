package main

var name string = "尼古拉斯赵四"
var age int = 16

type Person struct {
	classess int
}

func transform(name string) (newName string) {
	return name + "123"
}

const SYS = "IOS"

const (
	SYSTERM = "ios"
	TYPE    = false
)
